# Copy to .env and adjust if needed.
# When running the backend locally, the database must be running (see DATABASE_SETUP.md).

# Database (PostgreSQL must be running locally; see DATABASE_SETUP.md)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=election

# Server
PORT=3001
